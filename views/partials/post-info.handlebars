<div class="card">
  <div class="card-body">
    <a href="/post/{{id}}"><h5 class="card-title">{{title}}</h5></a>
    <h6 class="card-subtitle mb-2 text-muted">Posted by
      {{user.username}}
      on
      {{format_date created_at}}</h6>
    <p class="card-text">{{content}}</p>
  </div>
</div>